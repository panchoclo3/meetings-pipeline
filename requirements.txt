# --- Transcripción y diarización ---
# WhisperX tiene dependencias sensibles a versión (torch, ffmpeg). Si `pip install whisperx`
# falla, sigue las instrucciones oficiales: https://github.com/m-bain/whisperX
whisperx
torch
pyannote.audio>=3.1

# --- Extracción con Claude ---
anthropic>=0.40.0

# --- Notion API ---
requests>=2.31.0

# --- Validación de schema ---
jsonschema>=4.21.0

# --- Configuración y utilidades ---
pyyaml>=6.0
python-dotenv>=1.0.0
